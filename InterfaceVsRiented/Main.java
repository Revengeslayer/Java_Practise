package cm.code.InterfaceVsRiented;
/*
1 類與類是單繼承 只有一個父類 多個兄弟
2 類與interface 是多實現的 一個類有多個interface
3 interface與interface 是多繼承的

注意事項:
1 多個父interface的抽象函式如果重複 (O)
2 多個父interface的默認函式如果重複 子interface必須進行默認函式的override 必且有 default關鍵字
 */
public class Main {
}
